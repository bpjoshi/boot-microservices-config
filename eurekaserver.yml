server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false #dont fetch registry of other microservices
    registerWithEureka: false #No need to register itself
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    
